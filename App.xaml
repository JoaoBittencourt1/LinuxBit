<Application x:Class="LinuxHub.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LinuxHub"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <!-- O WPF-UI restiliza os controles nativos que ele reaproveita (RadioButton,
                 ComboBox, etc.) via ControlsDictionary, mas não o TextBlock puro — ele tem
                 seu próprio Wpf.Ui.Controls.TextBlock separado. Sem isso, todo TextBlock
                 simples renderiza com o Foreground padrão (preto), ilegível no tema escuro. -->
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
